# requirements.txt

# ...
Django==3.2.25
djongo==1.3.7
# CHANGE THIS LINE to a compatible version (3.11.4 is the maximum allowed)
pymongo[srv]==3.11.4 
# The [srv] is necessary for MongoDB Atlas SRV connection strings
dnspython==2.4.2 
gunicorn==21.2.0
whitenoise==6.6.0
python-dotenv==1.0.0
